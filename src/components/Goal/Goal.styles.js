import { StyleSheet } from "react-native";

export const goalStyles = StyleSheet.create({
  goal: {
    borderRadius: 10,
    margin: 10,
    padding: 10,
    backgroundColor: "#fff",
    borderRadius: 10,
  }
})